module.exports = () => (req, res, next) => {
  // You can add any logic that needs to be executed in this middleware
  // For example, logging or setting response headers

  // Simply pass control to the next middleware
  next();
}
